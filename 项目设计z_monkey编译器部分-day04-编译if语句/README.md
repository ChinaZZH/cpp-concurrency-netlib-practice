markdown

# Day04 monkey编译器  编译if语句

## 核心收获

-- 1. 支持编译if语句

-- 2. 编译if语句，需要支持 条件为假的时候跳转(code.OpJumpNotTruthy) 和 无条件跳转(code.OpJump)

-- 3. 由于写入跳转语句的时候还不知道具体要跳转到哪里，所以需要先记录当前序列的位置，当到底需要跳转的地址的时候再把该地址回填。

-- 4. 跳转采用的是相对偏移位置的跳转。就是 跳转语句结束后还需要跳过多少字节的指令才到 需要执行的执行的位置。(offset)



## 整个程序的核心

