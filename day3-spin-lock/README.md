markdown

# Day3： 自旋锁 (spin_lock)

## 核心收获
-- 1.通过原子变量 std::atomic<bool> 和 std::atomic_flag 实现自旋锁

-- 2.自旋锁的使用场景 是当知道加锁的时间极短并且短于线程切换的时候使用自旋锁，当加锁解锁的逻辑复杂并且不知道什么时候解锁的时候使用互斥锁。

-- 3.了解内存序。
## 代码
-- spin_lock.cpp

## 测试
-- 一切正常。
