
markdown

## Day13：  C++ 并发编程第一阶段总结。

## 一、基础同步原语（线程、mutex、条件变量、生产者-消费者）

## 1.线程管理
(1). std::thread：创建线程，传递参数时注意默认按值传递，若需要引用请使用 std::ref。

(2). 在std::thread对象被销毁前必须选择一种运行方式，join() 或者 detach(). 

1.join() 是这个std::thread是在当前依附的线程中等待自身线程std::thread的结束。
      
2.detach() 是这个std::thread是和当前依附的线程分离，自身由系统调度来决定线程何时结束。


-- (3). 线程id: 通过std::this_thread::get_id()获取.


## 2.互斥(mutex)
 (1). 互斥锁 std::mutex, std::lock_guard, std::unique_lock。

1.std::mutex 是保护临界区和共享变量的最基本的锁，lock和unlock配对使用并且需要显式的调用，如果lock了忘记了unlock则永远不会解锁其他线程也永远都不会获得需要的这个共享资源。
      
2.std::lock_guard是对std::mutex进行RAII的封装，构造的时候加锁，析构的时候解锁，不能去调用加锁和解锁的函数。用法简单不容易出错，是一个对std::mutex进行使用简化的产品。

3. std::unique_lock对std::lock_guard功能进行升级不仅支持构造的时候加锁，析构的时候解锁。同时可以手动的进行延迟加锁和提前解锁(没有特殊情况不建议)，还可以配合条件变量使用功能强大。

      
(2) 死锁：多个线程拥有对方想要的资源并且相当等待造成。 

1.形成死锁有四个条件: 资源处于互斥条件，资源不可剥夺条件，请求并保持， 循环等待。 四个条件中的任意一个不满足都不会形成死锁。

2.在工程上怎么避免死锁呢: 1. 资源的有序申请  2.给每个共享资源设置一个超时机制，持有超过该事件就释放  3.减少锁的粒度，尽量避免锁的申请和使用, 4.避免嵌套锁。


## 3. 条件变量
(1). std::condition_variable 需配合 std::unique_lock。


(2). wait() 必须检查条件（防止虚假唤醒）：

```cpp
      cv.wait(lock, []{ return !queue.empty(); });
```


(3). notify_one() 与 notify_all()：唤醒等待线程。


(4). 关注wait_for和wait_until的使用。

wait_for: 等待一段时间（相对时间）然后会被唤醒

wait_until: 等待到绝对时间点 然后会被唤醒

|函数|	超时类型|	返回值（无谓词）|	返回值（有谓词）|	典型场景|
|----|--------------|------------------|-----------------|----------|
|wait|	无	|void	|void	|无限等待，直到被显式唤醒|
|wait_for|	相对时间|	cv_status::timeout 或 no_timeout|	true（条件满足） / false（超时且条件不满足）|	等待资源最长不超过N秒|
|wait_until|	绝对时间|	同上	|同上	|等待到某个绝对 |deadline|

## 4. 生产者-消费者示例
  (使用 mutex + 条件变量）见 GitHub中的day2。

## 5. 其他
(1). std::call_once 可以确保在多线程环境下只被调用一次，可以用作与单例。

(2). 读写锁，用于读多写少的场景并且是读:写>=10:1的场景性能有明显提升。见github上的day08的代码示例及说明。

(3). std::recursive_mutex，递归锁，用于递归函数调用加相同类型的锁，解锁是反向的。慎用。

## 二、内存模型与原子操作（memory_order、自旋锁、CAS）
## 1.内存模型必要性
 (1).CPU 与编译器的指令重排会导致多线程下的可见性问题。

 (2).C++11 引入了原子库和内存序，帮助程序员控制顺序。

## 2.std::atomic<T>
(1).支持 store()、load()、exchange()、compare_exchange_weak/strong()、fetch_add() 等。

(2). 保证对基本类型的读写是原子的。

## 3.内存序
|内存序	            |读部分	      |写部分	|用途      |            主要用途|
|-----------------------|-----------------|-----------|----------|------------------|
|memory_order_relaxed	|relaxed	|relaxed	|仅原子性，无顺序保证|所有函数都使用|
|memory_order_acquire	|acquire	|relaxed	|读操作，之后读写不能重排到之前|load()、compare_exchange_weak/strong()、fetch_add()|
|memory_order_release	|acquire（隐含）	|release	|写操作，之前读写不能重排到之后|load()、compare_exchange_weak/strong()、exchange()|
|memory_order_acq_rel	|acquire	|release	|读‑改‑写操作，同时获取与释放|所有函数都使用|
|memory_order_seq_cst	|seq_cst	|seq_cst	|全局顺序一致性（默认）|所有函数都使用|

## 4.自旋锁
 (1).使用 std::atomic_flag 或 std::atomic<bool>）展示了 acquire/release 的典型用法 

(2).见 GitHub中的day3。

## 三、无锁数据结构（栈、队列、线程池）
(1).无锁栈版本号解法: 通过版本号递增避免相同的内存地址被重用。 ABA 防御：即使指针地址被重用，tag 已递增，CAS 失败。

(2).无锁栈引用计数解法: 通过引用计数方法中shared_ptr中的控制块的地址不同。 ABA 防御：即使指针地址被重用，控制块地址不同，CAS 失败。

 (3).无锁队列（MPSC，链表版） 适用于单一消费者场景

(4).线程池（支持 std::future）

(5).性能测试结论：当任务计算量较重时，线程池相比串行有明显加速；当任务极轻时，串行反而更快（调度开销主导）

(6).更多内容请看github相关。

## 四、ABA 问题与解决方案（版本号、风险指针、引用计数，带对比表）
(1).  ABA 问题本质
线程 T1 读取地址 A。

T2 将 A 改为 B 又改回 A（地址被释放后重新分配得到相同地址）。

T1 恢复后 CAS 成功，但 A 已不是原来的对象，导致数据错误。

(2). 解决方案一：版本号（Tagged Pointer）
原理：指针 + 递增 tag，CAS 同时比较指针和 tag。

优点：简单、高性能、彻底解决 ABA。

缺点：需要双字 CAS（x86‑64 支持，ARM 部分支持）；tag 可能溢出（64 位几乎不会）。

实现：无锁栈、无锁对象池。

(3).解决方案二：风险指针（Hazard Pointer）
原理：每个线程在访问节点前设置“风险指针”表示正在使用。删除前扫描所有风险指针，未被引用的节点才能安全删除。

优点：完全内存安全，无悬空指针。

缺点：实现复杂，需管理线程局部存储和全局列表，扫描开销高。

实现：MPSC / MPMC 队列简化版（见 GitHub）。

 (4). 解决方案三：引用计数（std::shared_ptr）
原理：std::shared_ptr 包含原始指针和控制块（含引用计数）。即使地址相同，控制块不同，CAS 也会失败。

优点：自动内存管理，实现简单。

缺点：std::atomic<std::shared_ptr<T>> 通常不是无锁的（C++20 前可能使用互斥锁），高竞争下性能低下。

实现：RefCountStack，仅用于理解概念。


|  方案  |    适用场景    |  实现难度  |  性能  |   内存安全   |     游戏应用例子     | 不适合的场景|优点|
|--------|----------------|------------|--------|-------------|------------------------------|-------------|------|
|版本号|	无锁栈、对象池、固定大小队列|低|极高|需手动回收|实体对象池、无锁栈|节点生命周期不可控、需自动回收内存|原理简单，能彻底解决 ABA（只要 tag 不溢出）|
|风险指针| 通用无锁队列、数据结构库|	高|中|完全安全|命令队列、更新队列|实现复杂度高、对延迟要求极高|真正安全，不会出现悬空指针|
|引用计数| 单向无锁结构|中|	中|安全（无循环）|技能链、共享资源|双向链表、环形结构（循环引用）|避免 ABA，自动内存管理（借助 shared_ptr）|


## 五、性能测试与思考（QPS 对比、任务轻重的影响）
(1). 没有一种实现是绝对最优的，需要根据任务特性选择。

 (2). 在游戏服务端，通常任务是中等或重度（例如 AI 计算、寻路、数据打包），此时无锁队列线程池会有明显优势。

(3). 如果任务极轻量（如累加几个整数），直接在主线程串行执行反而更好。

(4). 从下面的几个例子来看，无锁线程池都要优于有锁竞争的线程池.工作线程竞争队列时，无锁队列避免了互斥锁的阻塞和上下文切换，延迟更低，吞吐更高。

(5).  任务计算轻量的情况下(可能只有几十纳秒到几百纳秒)， 任务提交、队列操作、std::future 的创建、线程调度、缓存一致性等开销远大于任务本身的计算时间。


## 六、总结 & GitHub 链接
github地址 https://github.com/ChinaZZH/cpp-concurrency-netlib-practice


