
markdown

## Day13：  C++ 并发编程第一阶段总结。

## 一、基础同步原语（线程、mutex、条件变量、生产者-消费者）

## 1.线程管理
-- (1). std::thread：创建线程，传递参数时注意默认按值传递，若需要引用请使用 std::ref。

-- (2). 在std::thread对象被销毁前必须选择一种运行方式，join() 或者 detach(). 

      join() 是这个std::thread是在当前依附的线程中等待自身线程std::thread的结束。
      
      detach() 是这个std::thread是和当前依附的线程分离，自身由系统调度来决定线程何时结束。

-- (3). 线程id: 通过std::this_thread::get_id()获取.

## 2.互斥(mutex)
-- (1). 互斥锁 std::mutex, std::lock_guard, std::unique_lock。

      std::mutex 是保护临界区和共享变量的最基本的锁，lock和unlock配对使用并且需要显式的调用，如果lock了忘记了unlock则永远不会解锁其他线程也永远都不会获得需要的这个共享资源。
      
      std::lock_guard是对std::mutex进行RAII的封装，构造的时候加锁，析构的时候解锁，不能去调用加锁和解锁的函数。用法简单不容易出错，是一个对std::mutex进行使用简化的产品。

      std::unique_lock对std::lock_guard功能进行升级不仅支持构造的时候加锁，析构的时候解锁。同时可以手动的进行延迟加锁和提前解锁(没有特殊情况不建议)，还可以配合条件变量使用功能强大。
      
-- (2) 死锁：多个线程拥有对方想要的资源并且相当等待造成。 

      形成死锁有四个条件: 资源处于互斥条件，资源不可剥夺条件，请求并保持， 循环等待。 四个条件中的任意一个不满足都不会形成死锁。

      在工程上怎么避免死锁呢， 1. 资源的有序申请  2.给每个共享资源设置一个超时机制，持有超过该事件就释放  3.减少锁的粒度，尽量避免锁的申请和使用

## 3. 条件变量
-- (1). std::condition_variable 需配合 std::unique_lock。

-- (2). wait() 必须检查条件（防止虚假唤醒）：

```cpp
      cv.wait(lock, []{ return !queue.empty(); });
```

-- (3). notify_one() 与 notify_all()：唤醒等待线程。

-- (4). 关注wait_for和wait_until的使用。

      wait_for: 等待一段时间（相对时间）然后会被唤醒

      wait_until: 等待到绝对时间点 然后会被唤醒


## 二、内存模型与原子操作（memory_order、自旋锁、CAS）

## 三、无锁数据结构（栈、队列、线程池）

## 四、ABA 问题与解决方案（版本号、风险指针、引用计数，带对比表）

## 五、性能测试与思考（QPS 对比、任务轻重的影响）

## 六、总结 & GitHub 链接


## 核心收获
--  1. 没有一种实现是绝对最优的，需要根据任务特性选择。

--  2. 在游戏服务端，通常任务是中等或重度（例如 AI 计算、寻路、数据打包），此时无锁队列线程池会有明显优势。

--  3. 如果任务极轻量（如累加几个整数），直接在主线程串行执行反而更好。

--  4. 从下面的几个例子来看，无锁线程池都要优于有锁竞争的线程池.工作线程竞争队列时，无锁队列避免了互斥锁的阻塞和上下文切换，延迟更低，吞吐更高。

--  5.  任务计算轻量的情况下(可能只有几十纳秒到几百纳秒)， 任务提交、队列操作、std::future 的创建、线程调度、缓存一致性等开销远大于任务本身的计算时间。

## 代码
-- ThreadPool.cpp

## 测试

测试环境（公平版本）
CPU：8核 12 线程（典型开发机-本人笔记本电脑）

编译器：MSVC 2026，Release 模式，/O2 优化，/std:c++20

任务负载：int 平方累加，每个任务循环 10000 次（加重计算，凸显并行优势）

任务数量：50000

线程池线程数：12

预期性能对比（文本表格）  基准环境
|实现QPS tasks_sec   |   相对 Baseline      | 加速	             |		说明                                      |
|--------------------|----------------------|--------------------|----------------------------------------------|	
|Baseline (串行)      |  1, 885, 255        |  1.00x            |    单生产者单消费者，串行执行                  |	
|MutexThreadPool      |     536, 060        |  0.28x            |    并行，但有锁竞争                  |	
|LockfreeThreadPool   |  1, 924, 653        |  1.02x            |     并行，无锁，高并发优势显著                          |
                 
<img width="1176" height="109" alt="111" src="https://github.com/user-attachments/assets/7be62bb6-9f0d-4881-8d62-1b6e88b9729a" />


说明描述：LockfreeThreadPool QPS 最高 (1, 924, 653 tasks/sec)，比 Baseline 快 2% 只是有一点点的性能优势，但是MutexThreadPool只是BaseLine的0.23。


在基准环境下，任务数量：50000 保持不变   任务负载：int 平方累加，每个任务循环 10000 次（加重计算，凸显并行优势） 任务负载的循环从10000 变成 10000，每个任务的计算变的更重。
|实现QPS tasks_sec   |   相对 Baseline      | 加速	             |		说明                                      |
|--------------------|----------------------|--------------------|----------------------------------------------|	
|Baseline (串行)      |  283, 618        |  1.00x            |    单生产者单消费者，串行执行                  |	
|MutexThreadPool      |     1,195, 628        |  4.21x            |    并行，但有锁竞争                  |	
|LockfreeThreadPool   |  1, 413, 499        |  4.98x           |        并行，无锁，高并发优势显著                       |

<img width="1194" height="151" alt="222" src="https://github.com/user-attachments/assets/41562135-8379-45e2-9e04-b5aa7e0f07e3" />


高亮：LockfreeThreadPool QPS 最高 (1, 413, 499 tasks/sec)，比 Baseline 快 398%, 比MutexThreadPool快78%。
      说明如果任务是属于计算密集型的任务，计算比较重的任务，则并发的优势明显不管是有锁还是无锁。


在基准环境下，任务负载：循环10000 保持不变   任务数量：500000  任务数量从50000 变成 500000，任务数量变成10倍。
|实现QPS tasks_sec   |   相对 Baseline      | 加速	             |		说明                                      |
|--------------------|----------------------|--------------------|----------------------------------------------|	
|Baseline (串行)      |   2, 302, 935        |  1.00x            |    单生产者单消费者，串行执行                  |	
|MutexThreadPool      |     457, 735        |  0.19x            |   并行，但有锁竞争                   |	
|LockfreeThreadPool   |  1, 807, 718        |  0.78x           |     并行，无锁，高并发优势显著                          |

<img width="1204" height="99" alt="333" src="https://github.com/user-attachments/assets/60c76bf1-e906-437c-8645-cba87ff1ed70" />

高亮：Baseline串行计算 QPS 最高 (1, 807, 718 tasks/sec)，比 有锁线程池 快 81%, 比无锁线程池快22%。  
     说明如果任务不属于计算重的情况下，属于任务及其多的情况下，属于任务个数密集型的情况下则串行相比与有锁线程池优势明显，比无锁线程池也稍占优势

