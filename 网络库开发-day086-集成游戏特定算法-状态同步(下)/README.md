markdown

# Day86： 集成游戏特定算法  状态同步(下) -属性同步

## 核心收获

-- 1.   当有属性变更的时候，将属性更新为最新值。同时不马上进行同步 而是设置脏标记。
 
-- 2.  每隔一段时间发现数据为脏则同步差更改的属性值给客户端(同步差量值)，同时重制脏标记并且将变更的字段值存入对应的历史版本。

-- 3.  当客户端上来请求数据的时候，发送全量包给客户端。
 

### 测试
-- 1.  单元测试通过。


## 测试
-- 一切正常。