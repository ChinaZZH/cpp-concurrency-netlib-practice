markdown

# AOI算法 性能比对测试

## 性能比对

|算法|	核心特点|	最佳适用场景|	慎用场景|
|-----|-----------|---------------|-----------|
|网格法（九宫格）|	简单直接，按位置划分固定格子	| 平坦开阔地图、玩家分布相对均匀（如 MOBA、大逃杀）|	玩家高度聚集（如主城摆摊）、地图地形复杂多障碍|
|十字链表|	基于双向链表维护 X/Y 轴顺序，擅长遍历|	逻辑密集型场景、需要快速 AOI 遍历（如回合制游戏、策略游戏）|	高频创建/删除实体（如频繁进出副本）|
|四叉树|	自适应分区，根据对象密度动态调整	| 地形复杂、对象分布不均匀（如开放世界 RPG、场景中有大量障碍物）|	需要极高性能的实时竞技游戏（因为动态分区和移动更新开销较大）|
|动态AOI|	自适应分区，根据对象密度动态调整	| 地形复杂、对象分布不均匀（如开放世界 RPG、场景中有大量障碍物）|	需要极高性能的实时竞技游戏（因为动态分区和移动更新开销较大）|


-- 1.  在进行5000个不同的玩家登录地图， 10000次的玩家在地图里面移动， 5000次的查询视野内的玩家。同时地图设置长宽为 (10000x10000）。 视野范围为(150 乘以 150). 得到下面的性能分析数据。
  
 |算法	|插入 (ms)	|移动 (ms)	|查询 (ms)	|总耗时 (ms)	|综合评价|
|-------|-------------|-------------|-------------|-------------|---------|
|网格法	|3.69	|3.46	|14.71	| 21.85	|✅ 优秀|
|四叉树	|4.47	|425.06	|15.30 	|444.82	|省内存|
|十字链表	|301.31	|31.63	|1.53	|334.47	|查询最优|
|动态AOI_V1|	34.60|	19.30|	59.70|	113.60|省内存|
|动态AOI_V2(十字链表)|	225.97|	26.93|	71.10|	324.01|省内存|
|动态AOI_V2(九宫格)|	62.26|	24.91|	68.32|	155.49|省内存|
|动态AOI_V2(四叉树)|	55.55|	25.99|	68.16|	149.71|省内存|

-- 普通地图首选九宫格法



 |算法 |	核心思想	 | 内存占用	 | 查询性能 |	适用场景  |
 |-------|-------------|-------------|-------------|-------------|
 |网格法 （九宫格） |	固定网格，O(1) 定位	 | 高（全量分配） |	最高（直接索引） |	地图不大、内存充足，实体分布均匀， 没有特殊情境都选择这个，实现简单，性能稳定，综合表现最好 |
 |十字链表，一字链表 |	X/Y 轴双向链表，范围查询后求交集 |	低（只存实体） |	中等（需遍历链表 + 集合求交） |	实体数量适中、移动频繁，地图大但实体少(实体数量有限，查询高频) 还可以不用十字，而用一字链表 |
 |四叉树 |	按实体数量分裂 + 合并（可回收） |	中等（按需分配 + 按数量回收）	 | 中等（树遍历 + 叶子节点） |	地图大、实体分布不均匀，内存敏感 |
 |动态 AOI |	按密度分裂 + 合并（按面积回收） |	最低（按需分配 + 密度驱动回收） |	中等（树遍历 + 密度统计） |	大地图，实体分布不均，内存敏感， 密度波动剧烈，长期运行需动态调整 |


## 数据解读
###  为什么十字链表这么差？

-- 1. 十字链表的优势在于查询（O(1) 从当前节点向四个方向遍历），但它的插入和移动是O(n) 的链表遍历。

-- 2. 在 5000 个实体规模下，每次插入或移动都需要遍历平均 2500 个节点来找到正确的插入位置，累积起来就是 4500 毫秒的移动耗时。这意味着在实体数量超过几百的场景下，十字链表就已经不适合了。

-- 3. 它适用于“实体数量少、查询频繁”的逻辑密集型场景，比如回合制游戏或策略游戏，而在你现在的测试条件（5000 实体、大量移动）下，它的弱点被放大了。


###  为什么网格法最优？

-- 1. 网格法的插入、移动、查询都是 O(1) 的取模运算，没有复杂的数据结构维护。

-- 2.  移动 66ms 是因为每次移动都要更新两个网格的 set，删除和插入各一次，但整体仍然非常快。

-- 3.  网格法的查询是九宫格范围的固定扫描，23ms 属于正常范围。


##  结论与建议


-- 1.  网格法综合最优：如果实现简单、维护成本低是优先考虑因素，网格法仍然是不错的选择。

-- 2.  十字链表/一字链表 需要谨慎使用：只有在“实体数量少、查询极频繁、移动极少”的场景下（比如回合制战斗中的 AOI 遍历），十字链表才有优势。

-- 3. 四叉树/动态AOI：如果是大地图，实体分布不均， 内存敏感， 需要牺牲时间换空间则需要采用这个。