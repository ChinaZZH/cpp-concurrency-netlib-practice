markdown

# Day92： 集成游戏特定算法  防作弊加固(多维校验/惩罚机制/审计日志)

## 核心收获

-- 1.  多维校验 释放技能攻击是否已经超过了cd时间，如果还在cd时间内则记录则攻击返回失败，并且记录违规次数和日志。(通过服务端帧来判断)
 
-- 2.  多维校验 速度校验， 通过两次移动判断速度是否超过阈值。如果超过阈值则强制拉回并记录违规次数和日志。
 
-- 3.  多维校验 客户端帧号校验， 如果两次相邻的客户端帧同步包的帧号相差不止1则说明客户端发送有问题的数据。记录违规次数和日志。

-- 4.  进行违规惩罚，当违规次数超过三次的时候则发送矫正包强制拉回服务端权威位置。 当违规次数超过五次的时候则把玩家提出服务端。 当违规次数超过十次的时候则把玩家进入黑名单，禁止登录。


### 测试
-- 1.  测试通过。


## 测试
-- 一切正常。