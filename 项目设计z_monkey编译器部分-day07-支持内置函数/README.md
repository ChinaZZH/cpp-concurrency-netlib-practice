markdown

# Day07 monkey编译器  内置函数

## 核心收获

-- 1.  当标识符在符号表中找不到对应的符号对象的时候，这个时候就查下内置函数表。

-- 2.  内置函数表找到了对应的内置函数对象，则将内置放入常量表中。并且在指令集中放入opConstant指令。

-- 3.  运行常量指令的时候会先将函数常量放入操作数栈

-- 4.  在运行函数调用指令的时候判断函数名是否是内置函数对象，是的话通过参数执行直接出结果。

## 整个程序的核心

