markdown

## Day10： 性能测试	对比三种实现：std::mutex 队列、无锁队列、互斥锁线程池。
## 核心收获
-- 1. 关于线程池ThreadPool的讨论， 在原先的ThreadPool移除条件变量和互斥锁，将之前实现的基于无锁的mpmc队列应用到线程中去，同时将stop这个也是共享变量修改为std::atomic。

-- 2. 关于线程池ThreadPool的讨论， 在线程中的取任务动作中如果任务队列为空同时并没有结束这个流程，则通过std::this_thread::yield() 让其休眠一会儿让出CPU控制权。

## 代码
-- ThreadPool.cpp

## 测试

测试环境（公平版本）
CPU：48核 12 线程（典型开发机）

编译器：MSVC 2026，Release 模式，/O2 优化，/std:c++20

任务负载：int 平方累加，每个任务循环 10000 次（加重计算，凸显并行优势）

任务数量：50000

线程池线程数：12

预期性能对比（文本表格）  基准环境
|实现QPS tasks_sec   |   相对 Baseline      | 加速	             |		说明                                      |
|--------------------|----------------------|--------------------|----------------------------------------------|	
|Baseline (串行)      |  1, 885, 255        |  1.00x            |    单生产者单消费者，串行执行                  |	
|MutexThreadPool      |     536, 060        |  0.28x            |    并行，无锁，高并发优势显著                  |	
|LockfreeThreadPool   |  1, 924, 653        |  1.02x            |    并行，但有锁竞争                           |
                 
![本地图](C:\Users\56593\Desktop\111.png)

高亮：LockfreeThreadPool QPS 最高 (1, 924, 653 tasks/sec)，比 Baseline 快 2%。


在基准环境下，任务数量：50000 保持不变   任务负载：int 平方累加，每个任务循环 10000 次（加重计算，凸显并行优势） 任务负载的循环从10000 变成 10000，每个任务的计算变的更重。
|实现QPS tasks_sec   |   相对 Baseline      | 加速	             |		说明                                      |
|--------------------|----------------------|--------------------|----------------------------------------------|	
|Baseline (串行)      |  283, 618        |  1.00x            |    单生产者单消费者，串行执行                  |	
|MutexThreadPool      |     1,195, 628        |  4.21x            |    并行，无锁，高并发优势显著                  |	
|LockfreeThreadPool   |  1, 413, 499        |  4.98x           |    并行，但有锁竞争                           |

高亮：LockfreeThreadPool QPS 最高 (1, 413, 499 tasks/sec)，比 Baseline 快 398%, 比MutexThreadPool快78%。


在基准环境下，任务负载：循环10000 保持不变   任务数量：500000  任务数量从50000 变成 500000，任务数量变成10倍。
|实现QPS tasks_sec   |   相对 Baseline      | 加速	             |		说明                                      |
|--------------------|----------------------|--------------------|----------------------------------------------|	
|Baseline (串行)      |   2, 302, 935        |  1.00x            |    单生产者单消费者，串行执行                  |	
|MutexThreadPool      |     457, 735        |  0.19x            |    并行，无锁，高并发优势显著                  |	
|LockfreeThreadPool   |  1, 807, 718        |  0.78x           |    并行，但有锁竞争                           |

高亮：Baseline QPS 最高 (1, 807, 718 tasks/sec)，比 Baseline 快 398%, 比MutexThreadPool快78%。


